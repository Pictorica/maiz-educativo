# Supabase Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to git!

# Get these values from your Supabase project dashboard:
# Settings > API

# Your Supabase project URL
# Example: https://xxxxxxxxxxxxx.supabase.co
VITE_SUPABASE_URL=your-project-url-here

# Your Supabase anon/public key (safe for frontend use)
# Example: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# ⚠️ SECURITY NOTES:
# 1. The ANON key is safe to use in frontend code when Row Level Security (RLS) is enabled
# 2. NEVER use the SERVICE_ROLE key in frontend code
# 3. Add .env to your .gitignore file
# 4. For production deployment (Vercel, Netlify), set these as environment variables in your hosting dashboard
